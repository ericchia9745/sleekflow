spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:sleekflow_todo_test}?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: ${DB_USER:todo}
    password: ${DB_PASSWORD:todo_dev_pw}
  flyway:
    clean-disabled: false
