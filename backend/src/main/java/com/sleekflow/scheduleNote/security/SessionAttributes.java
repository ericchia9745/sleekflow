package com.sleekflow.scheduleNote.security;

public final class SessionAttributes {

	/** Request attribute holding the raw bearer token, for sign-out. */
	public static final String TOKEN = "scheduleNote.sessionToken";

	private SessionAttributes() {
	}
}
